---
import { Image } from 'astro:assets';
import { getContent, type Locale, t } from '@majiphaneer/data';
import heroWide from '../assets/photos/LK_01734-wide.jpg';
import Icon from '../components/Icon.astro';
import Base from '../layouts/Base.astro';
import { personJsonLd } from '../lib/jsonld.ts';
import { pagePath } from '../lib/paths.ts';
import { photoById } from '../lib/photos.ts';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const { home, about, contravia, books, music, gallery, contact } = getContent(locale);
// Canvas-extended (zoomed-out) derivative of the profile portrait, built for
// the wide hero crop; alt text comes from the source photo's record.
const heroAlt = photoById('profile').alt;
const origin = (Astro.site ?? new URL('http://localhost:4321')).origin;

// sarahjakesroberts.com-style home: clean photo hero, statement band, video
// (when provided), then full-bleed photo tiles that open the side pages.
const sections = [
  { href: pagePath(locale, 'about'), photo: photoById('floor-thoughtful'), title: about.title },
  {
    href: pagePath(locale, 'contravia'),
    photo: photoById('denim-laughing'),
    title: contravia.title,
  },
  { href: pagePath(locale, 'books'), photo: photoById('beige-smile'), title: books.title },
  { href: pagePath(locale, 'music'), photo: photoById('denim-table'), title: music.title },
  { href: pagePath(locale, 'gallery'), photo: photoById('suit-standing'), title: gallery.title },
  {
    href: pagePath(locale, 'contact'),
    photo: photoById('stool-seated'),
    title: contact.invitationsTitle,
  },
];
---

<Base locale={locale} page="home" jsonLd={[personJsonLd(origin, locale)]}>
  <section class="relative h-[58vh] min-h-[420px] overflow-hidden">
    <Image
      src={heroWide}
      alt={heroAlt[locale]}
      widths={[800, 1400, 2400]}
      sizes="100vw"
      loading="eager"
      fetchpriority="high"
      class="hero-img absolute inset-0 h-full w-full object-cover object-[50%_20%]"
    />
    <div
      class="pointer-events-none absolute inset-y-0 left-0 w-[22%] backdrop-blur-md [mask-image:linear-gradient(to_right,black_55%,transparent)]"
    >
    </div>
    <div
      class="pointer-events-none absolute inset-y-0 right-0 w-[22%] backdrop-blur-md [mask-image:linear-gradient(to_left,black_55%,transparent)]"
    >
    </div>
  </section>

  <section class="bg-highlight px-4 py-14 text-center">
    <div class="mx-auto max-w-6xl">
      <h1
        class="reveal mx-auto font-hero text-[clamp(0.95rem,2.5vw,1.9rem)] font-bold normal-case leading-[1.2] tracking-normal"
      >
        {home.heroIntro[0]}
      </h1>
      <div class="reveal reveal-d1 mt-6 flex flex-wrap justify-center gap-3">
        <a
          href={pagePath(locale, 'books')}
          class="inline-flex items-center gap-2 rounded-full bg-accent px-6 py-2.5 text-sm font-semibold text-on-accent transition-opacity hover:opacity-90"
        >
          {t(locale, 'cta.book')}
        </a>
        <a
          href={pagePath(locale, 'contravia')}
          class="inline-flex items-center gap-2 rounded-full border border-border px-6 py-2.5 text-sm font-semibold transition-colors hover:bg-surface"
        >
          {t(locale, 'cta.podcast')}
        </a>
      </div>
      <a
        href="#sections"
        class="scroll-cue reveal reveal-d2 mt-10 inline-flex flex-col items-center gap-1.5 text-xs font-semibold uppercase tracking-[0.35em] text-muted transition-colors hover:text-ink"
      >
        {t(locale, 'cta.more')}
        <Icon name="chevronDown" size={30} />
      </a>
    </div>
  </section>

  {
    home.welcomeVideoUrl !== '' && (
      <section class="bg-bg">
        <div class="mx-auto aspect-video w-full max-w-5xl">
          <iframe
            src={home.welcomeVideoUrl}
            title={home.welcomeTitle}
            loading="lazy"
            allowfullscreen
            class="h-full w-full"
          />
        </div>
      </section>
    )
  }

  <div id="sections" class="grid scroll-mt-4 md:grid-cols-2">
    {
      sections.map((section) => (
        <a href={section.href} class="reveal group relative block overflow-hidden">
          <Image
            src={section.photo.image}
            alt={section.photo.alt[locale]}
            widths={[600, 900, 1400]}
            sizes="(min-width: 768px) 50vw, 100vw"
            loading="lazy"
            class="aspect-[4/3] w-full object-cover object-[50%_25%] transition-transform duration-700 ease-out group-hover:scale-105"
          />
          <div class="absolute inset-0 bg-black/25 transition-colors duration-500 group-hover:bg-black/40" />
          <div class="absolute inset-x-0 top-3/4 flex -translate-y-1/2 justify-center px-6">
            <h2 class="text-center text-2xl text-white drop-shadow-md md:text-3xl">
              {section.title}
              <span class="mx-auto mt-4 block h-px w-16 bg-white/80" />
            </h2>
          </div>
        </a>
      ))
    }
  </div>
</Base>
