#!/usr/bin/env bash
set -euo pipefail
FILES=$(git diff --name-only --cached --diff-filter=ACMRT | grep -E "\.(ts|tsx|astro|js|jsx|css|json|md)$" || true)
[ -z "$FILES" ] && exit 0
npx biome format --write $FILES
