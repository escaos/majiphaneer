pre-commit:
  commands:
    biome:
      glob: '*.{js,mjs,ts,tsx,json,jsonc,css,astro}'
      run: pnpm biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: pnpm turbo typecheck
    test:
      run: pnpm turbo test
